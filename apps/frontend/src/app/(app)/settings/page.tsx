import { PageHeader } from '@/components/layout/PageHeader';

export default function SettingsPage() {
  return (
    <>
      <PageHeader title="설정" subtitle="계정 / 환경 설정 (준비 중)" />
      <div className="px-7 py-12 text-center text-[12.5px] text-text-3">
        설정 UI는 곧 추가됩니다.
      </div>
    </>
  );
}
