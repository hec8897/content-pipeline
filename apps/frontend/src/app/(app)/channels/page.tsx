import { PageHeader } from '@/components/shell/PageHeader';

export default function ChannelsPage() {
  return (
    <>
      <PageHeader title="채널" subtitle="네이버 블로그 / 인스타그램 연결 (준비 중)" />
      <div className="px-7 py-12 text-center text-[12.5px] text-text-3">
        채널 연결 UI는 곧 추가됩니다.
      </div>
    </>
  );
}
